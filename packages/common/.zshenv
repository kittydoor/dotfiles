# Always source ~/.profile for system setup
source $HOME/.profile

# XDG Base Directory Shim (uncomment to enable)
# With the exception of ~/.zshenv, it will read all dotfiles for zsh
# from configured directory. Can set in /etc/zsh/zshenv to also move ~/.zshenv.
# ZDOTDIR=$HOME/.config/zsh
