---
# roles/software/tasks/cli_tools.yml

- name: install terminal packages
  dnf:
    name: "{{ item }}"
    state: present
  loop:
    - vim-enhanced
    - neovim
    - htop
    - ranger
    - tldr
    - lshw
    - ncdu
    - parallel
    - httpie
    - curl
    - wget
    - stow
    - info
    - stress
    - ripgrep
  become: yes

- name: cite parallel
  shell: echo 'will cite' | parallel --citation
  changed_when: false
