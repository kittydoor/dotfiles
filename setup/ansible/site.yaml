---
- name: desktop configuration
  hosts: localhost
  become: no

  vars_prompt:
    - name: hostname
      prompt: "Hostname (leave empty for no change)"
      private: no

  roles:
    - role: hostname
      tags:
        - hostname
        - interactive

    - role: media
      tags: media

    - role: git
      tags: git

    - role: software
      tags: software

    - role: desktop
      tags: desktop

    - role: python_means_python3
      tags: python_means_python3

    - role: services
      tags: services

    - role: libvirt
      tags: libvirt
