ls /sys/firmware/efi/efivars # check if in uefi mode

# ping archlinux.org
# else, wifi-menu, and ping again

timedatectl set-ntp true

# tmux for convenience?
git clone https://gitlab.com/kittydoor/dotfiles.git
